import React from "react";
import { Card, CardTitle, CardBody, CardText } from "reactstrap";

const Chart = ({ cubejsApi, title, query, render }) => (
  <Card>
    <CardBody>
      <CardText>sds</CardText>
    </CardBody>
  </Card>
);

export default Chart;
